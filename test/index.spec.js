var Reflux = require('../src/index.js');
var app = Reflux({debug:true});
describe("Reflux Initialization", function(){

	it("should start the internal root stream",function(){
		expect(true).to.equal(false);
	});



});
