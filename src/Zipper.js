/**
The zipper acts as a central observer, or main reflux stream coordinator initializing the schema, and managing contexts
it binds and unbinds context streams into a subscribable and queryable schema
*/
